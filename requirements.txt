# Pinned to the EXACT versions used to build TerraOps.
# Why '==' and not '>=': reproducibility. A '>=' lets a future `pip install`
# pull a newer (possibly breaking) major version, so "same code" would run on a
# different stack and could produce different results. Pinning freezes the stack.
# Python: 3.12.x

# --- Deep learning ---
torch==2.10.0
torchvision==0.25.0
pillow==12.1.1

# --- Data / metrics ---
numpy==2.4.3
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
tqdm==4.67.3

# --- Config ---
pyyaml==6.0.3

# --- MLOps: DVC with S3 extra (MinIO speaks the S3 protocol) ---
dvc[s3]==3.67.1

# --- MLOps: MLflow client — pinned to the SAME version as the server image
# (Dockerfile.mlflow) to avoid client/server API drift.
mlflow==3.4.0

# --- Serving: FastAPI app (src/api.py) ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32        # required by FastAPI to parse UploadFile (multipart)

# --- Monitoring (Sprint 4) ---
# Deliberately absent from requirements-api.txt: drift reporting is a BATCH job
# (a scheduled run over the prediction log), not part of the request path.
# Putting evidently + pandas in the serving image would add hundreds of MB to a
# container that would never call them.
pandas==2.3.3                    # evidently's interface is DataFrame-based
evidently==0.7.21
psycopg2-binary==2.9.11          # reads the prediction log (same client as the API)
prometheus-client==0.25.0        # shared with the API image (src/metrics.py)

# --- Tests / lint ---
pytest==9.1.1
httpx==0.28.1                    # FastAPI TestClient transport (used in tests/)
ruff==0.16.2                     # pinned: a newer ruff can add rules and turn a
                                 # green CI red without a single code change
