# --- Python ---
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/
.env
.pytest_cache/

# --- DVC-tracked data & model artifacts ---
# On NE commite PAS les octets lourds dans Git. DVC les gère :
# - le dataset brut -> data/raw (le pointeur data/raw.dvc, lui, EST commité)
# - les sorties du pipeline (models/, gros artefacts) sont auto-ignorées par DVC
#   via des .gitignore générés dans dvc.yaml. On garde ici une ceinture de sécurité :
/data/raw
/models/*.pth

# --- Logs & scratch ---
logs/
*.log
logs_*.txt

# --- Sprint 4 monitoring: generated output vs anchors ---
# COMMITTED (deliberately): monitoring/reference.json is the frozen drift
# reference — the input-side twin of gate/frozen_val.json. It must be versioned,
# because every past drift verdict is only interpretable against the exact
# reference that produced it.
# NOT committed: everything below is regenerated on every run.
# timestamped Evidently HTML + JSON, one pair per report run
monitoring/reports/
# CT streak/cooldown state: runtime, machine-local
monitoring/trigger_state.json
